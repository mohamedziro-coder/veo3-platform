"use client";import { useState, useRef, useEffect } from "react";import { useRouter } from "next/navigation";import { motion, AnimatePresence } from "framer-motion";import { Mic, Play, Square, Download, Wand2, Volume2, AlertCircle } from "lucide-react";import { COSTS, deductCredits, getUserCredits } from "@/lib/credits";// Runway preset voices mapped by language + genderconst LANGUAGES = [    { code: "en", label: "English 🇬🇧" },    { code: "ar", label: "Arabic 🇸🇦" },    { code: "fr", label: "French 🇫🇷" },    { code: "es", label: "Spanish 🇪🇸" },    { code: "de", label: "German 🇩🇪" },    { code: "it", label: "Italian 🇮🇹" },    { code: "pt", label: "Portuguese 🇧🇷" },    { code: "nl", label: "Dutch 🇳🇱" },    { code: "ru", label: "Russian 🇷🇺" },    { code: "ja", label: "Japanese 🇯🇵" },    { code: "zh", label: "Chinese 🇨🇳" },    { code: "hi", label: "Hindi 🇮🇳" },    { code: "tr", label: "Turkish 🇹🇷" },    { code: "ko", label: "Korean 🇰🇷" },];// Map lang+gender → Runway preset voiceconst VOICE_MAP: Record<string, { man: string; woman: string }> = {    en: { man: "James", woman: "Leslie" },    ar: { man: "Liam", woman: "Maya" },    fr: { man: "Bernard", woman: "Olivia" },    es: { man: "Noah", woman: "Claudia" },    de: { man: "Martin", woman: "Marlene" },    it: { man: "Elias", woman: "Paula" },    pt: { man: "Frank", woman: "Mariah" },    nl: { man: "Mark", woman: "Lara" },    ru: { man: "Ragnar", woman: "Sandra" },    ja: { man: "Arjun", woman: "Serene" },    zh: { man: "Kendrick", woman: "Kiana" },    hi: { man: "Arjun", woman: "Rina" },    tr: { man: "Elias", woman: "Eleanor" },    ko: { man: "James", woman: "Katie" },};function resolveVoice(lang: string, gender: "man" | "woman"): string {    return VOICE_MAP[lang]?.[gender] || (gender === "man" ? "James" : "Leslie");}export default function VoicePage() {    const router = useRouter();    const [isPageLoading, setIsPageLoading] = useState(true);    const [text, setText] = useState("");    const [isLoading, setIsLoading] = useState(false);    const [audioUrl, setAudioUrl] = useState<string | null>(null);    const [error, setError] = useState<string | null>(null);    const [isPlaying, setIsPlaying] = useState(false);    const [selectedLang, setSelectedLang] = useState("en");    const [selectedGender, setSelectedGender] = useState<"man" | "woman">("woman");    const [speakingRate, setSpeakingRate] = useState(1.0);    const [pitch, setPitch] = useState(0.0);    const audioRef = useRef<HTMLAudioElement | null>(null);    const currentCredits = getUserCredits();    const canAfford = currentCredits >= COSTS.VOICE;    useEffect(() => {        const user = localStorage.getItem('current_user');        if (!user) {            router.push('/login');        } else {            setIsPageLoading(false);        }    }, [router]);    useEffect(() => {        if (audioRef.current) {            audioRef.current.onended = () => setIsPlaying(false);        }    }, [audioUrl]);    // Show loading while checking auth (AFTER all hooks)    if (isPageLoading) {        return (            <div className="min-h-screen bg-white flex items-center justify-center">                <div className="w-8 h-8 border-2 border-primary border-t-transparent rounded-full animate-spin" />            </div>        );    }    const containerVariants = {        hidden: { opacity: 0 },        visible: {            opacity: 1,            transition: { staggerChildren: 0.1 }        }    };    const itemVariants = {        hidden: { opacity: 0, y: 20 },        visible: { opacity: 1, y: 0 }    };    const handleGenerate = async () => {        if (!text) return;        if (!canAfford) {            setError(`Insufficient credits (${currentCredits} available, ${COSTS.VOICE} required)`);            return;        }        setIsLoading(true);        setAudioUrl(null);        setError(null);        setIsPlaying(false);        try {            const voicePreset = resolveVoice(selectedLang, selectedGender);            const response = await fetch("/api/generate-audio", {                method: "POST",                body: JSON.stringify({                    text,                    voiceId: voicePreset,                    languageCode: selectedLang,                    speakingRate,                    pitch,                    userEmail: JSON.parse(localStorage.getItem('current_user') || '{}').email                }),                headers: { "Content-Type": "application/json" }            });            const data = await response.json();            if (!response.ok) {                // Handle HTTP errors (401, 402, 400, 500, etc.)                setError(data.error || `Server error: ${response.status}`);                return;            }            if (data.success && data.audioContent) {                // Create Blob from base64                const audioBlob = await fetch(`data:audio/mp3;base64,${data.audioContent}`).then(r => r.blob());                const url = URL.createObjectURL(audioBlob);                setAudioUrl(url);                // Deduct Credits & Update Local State                if (data.credits !== undefined) {                    // Update user object in localStorage                    const user = JSON.parse(localStorage.getItem('current_user') || '{}');                    user.credits = data.credits;                    localStorage.setItem('current_user', JSON.stringify(user));                    // Trigger storage event for other components                    window.dispatchEvent(new Event('storage'));                    window.dispatchEvent(new Event('credits-updated'));                } else {                    // Fallback to client-side deduction if server doesn't return credits (shouldn't happen now)                    deductCredits(COSTS.VOICE);                }                // Log Activity to Database                const user = JSON.parse(localStorage.getItem('current_user') || '{}');                try {                    await fetch('/api/activity', {                        method: 'POST',                        headers: { 'Content-Type': 'application/json' },                        body: JSON.stringify({                            userEmail: user.email,                            userName: user.name,                            tool: 'Voice',                            details: `Generated voice: "${text.substring(0, 50)}${text.length > 50 ? '...' : ''}"`                        })                    });                } catch (activityError) {                    console.error('Failed to log activity:', activityError);                    // Don't fail the whole operation if activity logging fails                }            } else {                setError(data.error || "Failed to generate audio. Ensure Text-to-Speech API is enabled.");            }        } catch (err: any) {            console.error("Voice generation error:", err);            setError(err.message || "Network connection error.");        } finally {            setIsLoading(false);        }    };    const togglePlayback = () => {        if (!audioRef.current || !audioUrl) return;        if (isPlaying) {            audioRef.current.pause();            setIsPlaying(false);        } else {            audioRef.current.play();            setIsPlaying(true);        }    };    return (        <main className="min-h-screen flex flex-col items-center justify-center p-4 md:p-8 relative overflow-hidden pt-24 pb-32 bg-gray-50">            {/* Background Ambience */}            <div className="fixed inset-0 pointer-events-none">                <div className="absolute top-[-20%] left-[20%] w-[50%] h-[50%] rounded-full bg-blue-500/5 blur-[150px]" />                <div className="absolute bottom-[-10%] right-[10%] w-[60%] h-[60%] rounded-full bg-cyan-500/5 blur-[150px]" />            </div>            <motion.div                variants={containerVariants}                initial="hidden"                animate="visible"                className="w-full max-w-4xl z-10 flex flex-col items-center gap-8"            >                {/* Header */}                <motion.div variants={itemVariants} className="text-center space-y-4">                    <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-white border border-gray-200 backdrop-blur-md text-xs font-medium text-cyan-600 shadow-sm">                        <Volume2 className="w-3 h-3" />                        <span>Virezo Voice Studio</span>                    </div>                    <h1 className="text-5xl md:text-6xl font-bold tracking-tighter text-gray-900">                        Voice Over API                    </h1>                    <p className="text-gray-500 text-lg max-w-2xl mx-auto leading-relaxed">                        Transform text to professional voice with Neural2 technology. <br />                        <span className="text-gray-400 text-sm">Supports Arabic, English, French, German, Spanish & more.</span>                    </p>                </motion.div>                {/* Error Alert */}                <AnimatePresence>                    {error && (                        <motion.div                            initial={{ opacity: 0, height: 0 }}                            animate={{ opacity: 1, height: "auto" }}                            exit={{ opacity: 0, height: 0 }}                            className="w-full max-w-lg"                        >                            <div className="bg-red-500/10 border border-red-500/20 text-red-200 px-6 py-4 rounded-2xl flex items-center gap-3 backdrop-blur-md">                                <AlertCircle className="w-5 h-5 flex-shrink-0 text-red-500" />                                <div>                                    <p className="text-sm font-medium">{error}</p>                                    {(error.includes("API Key") || error.includes("Configure")) && (                                        <button                                            onClick={() => router.push('/admin')}                                            className="text-xs text-red-300 underline mt-1 hover:text-red-100"                                        >                                            Go to Admin Settings                                        </button>                                    )}                                </div>                            </div>                        </motion.div>                    )}                </AnimatePresence>                {/* Main Interface */}                <motion.div variants={itemVariants} className="w-full bg-white border border-gray-200 shadow-xl rounded-[2rem] p-1 md:p-2 relative overflow-hidden">                    <div className="bg-white rounded-[1.8rem] overflow-hidden p-6 md:p-8 space-y-6">                        {/* Language + Gender Dropdowns */}                        <div className="flex flex-col sm:flex-row gap-4">                            {/* Language */}                            <div className="flex-1 space-y-1">                                <label className="text-xs font-semibold text-gray-700 uppercase tracking-wide">Language</label>                                <select                                    value={selectedLang}                                    onChange={(e) => setSelectedLang(e.target.value)}                                    className="w-full bg-white border border-gray-200 rounded-xl px-4 py-3 text-gray-900 focus:border-primary focus:ring-2 focus:ring-primary/10 focus:outline-none transition-all text-sm cursor-pointer"                                >                                    {LANGUAGES.map((l) => (                                        <option key={l.code} value={l.code}>{l.label}</option>                                    ))}                                </select>                            </div>                            {/* Gender */}                            <div className="w-full sm:w-44 space-y-1">                                <label className="text-xs font-semibold text-gray-700 uppercase tracking-wide">Voice Type</label>                                <div className="flex rounded-xl border border-gray-200 overflow-hidden">                                    <button                                        onClick={() => setSelectedGender("man")}                                        className={`flex-1 py-3 text-sm font-semibold transition-all flex items-center justify-center gap-1.5 ${selectedGender === "man"                                            ? "bg-primary text-white"                                            : "bg-white text-gray-500 hover:bg-gray-50"                                            }`}                                    >                                        👨 Man                                    </button>                                    <button                                        onClick={() => setSelectedGender("woman")}                                        className={`flex-1 py-3 text-sm font-semibold transition-all flex items-center justify-center gap-1.5 border-l border-gray-200 ${selectedGender === "woman"                                            ? "bg-primary text-white"                                            : "bg-white text-gray-500 hover:bg-gray-50"                                            }`}                                    >                                        👩 Woman                                    </button>                                </div>                            </div>                        </div>                    </div>                {/* Voice Preview Info */}                <div className="p-4 bg-gradient-to-r from-blue-50 to-cyan-50 rounded-xl border border-blue-100 flex items-center gap-3">                    <div className="w-9 h-9 rounded-lg bg-gradient-to-br from-blue-500 to-cyan-500 flex items-center justify-center flex-shrink-0">                        <Mic className="w-4 h-4 text-white" />                    </div>                    <div>                        <p className="text-sm font-semibold text-gray-800">                            {selectedGender === "man" ? "👨" : "👩"} {LANGUAGES.find(l => l.code === selectedLang)?.label} — {selectedGender === "man" ? "Male" : "Female"} Voice                        </p>                        <p className="text-xs text-gray-500">Preset: <span className="font-mono text-blue-600">{resolveVoice(selectedLang, selectedGender)}</span></p>                    </div>                </div>                {/* Text Input */}                <label className="text-sm font-semibold text-gray-700 ml-1 mb-2 block">Script</label>                <textarea                    placeholder="Enter your script here... (e.g., 'Hello, how are you? This is a test of Voice Generation.')"                    value={text}                    onChange={(e) => setText(e.target.value)}                    className="w-full h-40 bg-gray-50 rounded-xl p-4 border border-gray-200 text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary text-xl resize-none leading-relaxed shadow-inner"                />                {/* Controls */}                <div className="flex justify-between items-center border-t border-gray-100 pt-6">                    <div className="text-xs text-gray-500 font-mono">                        {text.length} chars                    </div>                    <button                        onClick={handleGenerate}                        disabled={!text || isLoading}                        className="px-8 py-3 rounded-xl bg-gradient-to-r from-blue-500 to-cyan-500 font-bold text-white shadow-lg shadow-cyan-900/20 hover:shadow-cyan-500/40 disabled:opacity-50 disabled:cursor-not-allowed transition-all transform hover:scale-105 active:scale-95 flex items-center gap-2"                    >                        {isLoading ? (                            <>                                <div className="w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin" />                                <span>Generating...</span>                            </>                        ) : (                            <>                                <Mic className="w-5 h-5" />                                <span>Generate Voice ({COSTS.VOICE} Credits)</span>                            </>                        )}                    </button>                </div>            </motion.div>            {/* Audio Player Result */}            <AnimatePresence>            </AnimatePresence>            {/* --- NEW CONTENT SECTIONS --- */}            <div className="mt-40 w-full max-w-6xl">                <div className="grid grid-cols-1 md:grid-cols-2 gap-16 items-center">                    <div>                        <h2 className="text-4xl md:text-5xl font-black text-gray-900 mb-6 leading-tight">                            Human-Level <br /> <span className="text-cyan-600">Voice Synthesis</span>                        </h2>                        <p className="text-xl text-gray-500 mb-8 leading-relaxed">                            Stop using robotic text-to-speech. Our Neural2 engine captures breath, intonation, and emotion, making it indistinguishable from a real human recording.                        </p>                        <ul className="space-y-4">                            {["Hyper-realistic breathing & pausing", "Emotion control (Happy, Serious, Calm)", "Multi-language & local dialects", "Instant generation"].map((item, i) => (                                <li key={i} className="flex items-center gap-3 text-lg font-medium text-gray-700">                                    <div className="w-6 h-6 rounded-full bg-cyan-100 flex items-center justify-center text-cyan-600 text-xs">✓</div>                                    {item}                                </li>                            ))}                        </ul>                    </div>                    <div className="bg-white p-8 rounded-[2.5rem] shadow-2xl shadow-cyan-900/10 border border-gray-100 transform rotate-2 hover:rotate-0 transition-transform duration-500">                        {/* Fake waveform viz */}                        <div className="flex items-center justify-center gap-1 h-32 mb-6">                            {[...Array(20)].map((_, i) => (                                <div key={i} className="w-2 bg-gradient-to-t from-blue-500 to-cyan-400 rounded-full" style={{ height: `${Math.random() * 100}%` }} />                            ))}                        </div>                        <div className="text-center">                            <div className="text-2xl font-black text-gray-900">Crystal Clear Audio</div>                            <div className="text-gray-400 font-mono text-sm mt-1">48kHz • 320kbps • Stereo</div>                        </div>                    </div>                </div>            </div>            {/* Use Cases */}            <div className="mt-32 w-full max-w-6xl">                <h2 className="text-4xl font-black text-center text-gray-900 mb-16">Perfect For...</h2>                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">                    {[                        { title: "YouTube Videos", icon: "📺" },                        { title: "Audiobooks", icon: "📚" },                        { title: "Game Characters", icon: "🎮" },                        { title: "Marketing Ads", icon: "📣" }                    ].map((card, i) => (                        <div key={i} className="bg-white p-8 rounded-[2rem] border border-gray-100 hover:shadow-xl transition-all text-center group">                            <div className="text-4xl mb-4 transform group-hover:scale-110 transition-transform">{card.icon}</div>                            <h3 className="text-xl font-bold text-gray-900">{card.title}</h3>                        </div>                    ))}                </div>            </div>            {/* FAQ */}            <div className="mt-32 w-full max-w-4xl pb-20">                <div className="text-center mb-12">                    <h2 className="text-3xl md:text-4xl font-black text-gray-900">Voice Studio FAQ</h2>                </div>                <div className="grid grid-cols-1 md:grid-cols-2 gap-6">                    {[                        { q: "Can I use these voices on YouTube?", a: "Yes! You have full commercial rights to monetize any content created with our platform." },                        { q: "Do you support custom voice cloning?", a: "Currently in beta. Contact our enterprise support if you need to clone your own voice." },                        { q: "How many languages are supported?", a: "We support 25+ languages incuding Arabic, French, Spanish, German, and Japanese." },                        { q: "Is it expensive?", a: "No, it's significantly cheaper than hiring a voice actor. You pay only for the seconds you generate." }                    ].map((item, i) => (                        <div key={i} className="bg-white border border-gray-200 p-6 rounded-2xl">                            <h4 className="font-bold text-gray-900 text-lg mb-2">{item.q}</h4>                            <p className="text-gray-500 text-sm leading-relaxed">{item.a}</p>                        </div>                    ))}                </div>            </div>        </motion.div>        </main >    );}